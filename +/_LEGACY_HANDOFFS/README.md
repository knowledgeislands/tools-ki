# Legacy inbound handoffs

This retained historical area holds inbound handoff records created before the typed `ki-trades` contract. It is not an active trade directory; new inbound records belong in `+/_TRADES/`.

Preserve the legacy payloads as historical evidence. Do not use them to create, receive, release, or prune a typed trade.
