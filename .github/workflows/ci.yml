name: CI

on: [push, pull_request]

jobs:
  shell:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: brew install bats-core shellcheck
      - run: shellcheck bin/ki install.sh
      - run: bats tests
